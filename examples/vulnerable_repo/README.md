This bundled fixture contains intentionally fake, non-production values and
static Python examples for demonstrating LeakShield detections. It is safe
demo data; do not replace it with real credentials or tokens.

Run from the repository root:

```powershell
python -m leakshield examples\vulnerable_repo
```
